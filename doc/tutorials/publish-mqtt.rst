publish-mqtt
============

After compilation the tool `raduino-mqtt-client`

.. code-block:: sh

    raduino-mqtt-client
       -n : gateway address
       -T : encrypt messages to rf node
       -h : print this text

this tool will connect to `localhost:1883`. In future versions this will hopefully be configurable