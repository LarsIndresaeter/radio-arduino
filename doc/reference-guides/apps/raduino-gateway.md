# raduino-gateway

## help menu

```console
raduino-gateway
       -K <key> : encrypt command with transport key
             -C : print counter values
             -n : reboot gateway as node
             -h : print this text
```

