# raduino-mqtt-client

## help menu

```console
raduino-mqtt-client
           -K <key> : encrypt command with transport key
       -N <address> : wakeup node address
       -n <address> : gateway address
                 -h : p rint this text
```

