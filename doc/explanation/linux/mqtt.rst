. mqtt

the linux cli tool `raduino-mqtt` is a `mqtt client` that connects to a `mqtt broker`. The client will read data from a `gateway` and `node` and publish it to the `mqtt broker`. The client will subscribe to a topic on the broker and execute commands.
