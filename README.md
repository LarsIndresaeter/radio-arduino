# radio-arduino

## docker

1. install docker
2. create image

```bash
    ./tools/docker/build_in_docker.sh dockerbuild
```

## build

run your first build

```bash
    source tools/setup.sh docker
    raduino all
```

learn more

```bash
    source tools/setup.sh docker
    raduino help
```
